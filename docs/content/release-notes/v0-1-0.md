---
title: v0.1.0
date: 2026-06-14
---

First release.

## What's in it

`bsky` reads public Bluesky data through the AT Protocol AppView API at
`public.api.bsky.app`. No key, no account, no OAuth required.

### Commands

| command | what it does |
|---|---|
| `bsky profile <handle>` | Full profile (followers, bio, post count) |
| `bsky user <handle>` | A user's recent posts with engagement counts |
| `bsky search <query>` | Search Bluesky users by keyword |
| `bsky feeds` | Popular feed generators |
| `bsky trending` | Trending topics |
| `bsky thread <at-uri>` | Post thread, traversed recursively (`--depth`) |
| `bsky followers <handle>` | Follower list |
| `bsky following <handle>` | Following list |
| `bsky starter-packs <handle>` | Starter packs created by a user |

All commands support `-o table|json|jsonl|csv|tsv|url|raw`, `--fields`,
`--template`, `-n/--limit`.

## Install

```bash
go install github.com/tamnd/bluesky-cli/cmd/bsky@v0.1.0
```

Or download a prebuilt binary from the [releases page](https://github.com/tamnd/bluesky-cli/releases/tag/v0.1.0).
